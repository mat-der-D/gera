# CLAUDE.md

## Namazu を使わない

このリポジトリでは、特別な場合を除いて Namazu を利用しない。

日本語の文書を書いたあとに Namazu へ推敲を投げる工程を挟まない。文章の質は Claude が自分で担保する。

**例外は、人間が明示的に Namazu の利用を指示した場合のみ。**それ以外では、確認を取らずに使わない。

これは `~/CLAUDE.md` の「人間が読む日本語の文書は必ず Namazu に通す」という規約を、このリポジトリに限って上書きするものである。

## ソースのコメントは英語で書く

`src/` と `src-tauri/` のコメントは英語で書く（2026-09-05、コミット `03d8629` で全面的に切り替えた）。

**読み手が Claude だからである。**このリポジトリのコメントには設計の判断と実測値がそのまま入っていて、次に編集する Claude がそれを読んで動く。`~/CLAUDE.md` の「言語は読み手で決める」がそのまま当たる。コミットメッセージは元から英語である。

**日本語のまま残すものが三つある。**

- **画面に出る文字列すべて。**アプリの UI は日本語である——`keys.ts` の `GROUPS`、帯のメッセージ、`lib.rs` が返すエラー。**これらはコメントではなくデータである**
- **本人の発言の引用。**訳すと引用でなくなる。原文のまま置いて、英語の注釈を後ろに付ける
- **日本語そのものが題材の例。**半角カナ（`ﾊﾞ` → `バ`）、太字修正の正規表現が対象にする約物（`。` `、` `）` `」`）

**`docs/` と `presets/` は日本語のままである。**読み手が本人と友人だからで、`docs/CSS.md` は技術者でない読み手に向けて書いてある。
