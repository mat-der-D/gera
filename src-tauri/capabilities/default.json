{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "gera が必要とする最小の権限。ファイル入出力は自作コマンド (read_file / write_file) で行うため fs プラグインは含めない。core:window:allow-set-title は src/host.ts の setWindowTitle が使う。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "dialog:allow-open",
    "dialog:allow-save",
    "clipboard-manager:allow-write-text",
    "window-state:default"
  ]
}
